# Templaterized Links Generator

Basically, given a url containing variable, we are generating fully functional urls with variables replaced by values in from the same data files (see {}) or, by using default values taken from the local storage ([]).

This software is useful for exchanging setups for testing, It makes it easy to repeat test using links containing user ID'd, contract numbers, gloo routes, etc.